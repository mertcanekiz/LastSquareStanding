language: cpp
compiler:
  - gcc
  - clang
before_install:
  - if [ $TRAVIS_OS_NAME == linux ]; then sudo apt-get install -y libsdl2-dev libsdl2-image-dev cmake; fi
script: cd build && cmake ../ && make
